{
  "dependencies": {
    "async": "^2.1.2",
    "aws-sdk": "^2.6.12",
    "lodash": "^4.16.4",
    "node-pushnotifications": "git+https://github.com/Ryanthegiantlion/node-pushnotifications.git",
    "sumologic": "^1.0.0"
  },
  "devDependencies": {
    "make-up": "^9.3.1",
    "node-lambda": "^0.8.11"
  },
  "scripts": {
    "start": "node-lambda run",
    "test": "npm test",
    "lint": "make-up script src",
    "build": "cp template.env .env && ./script/generate_config.sh >> .env",
    "ci": "npm run lint && npm run build && npm run deploy",
    "deploy": "TMPDIR=/tmp node-lambda deploy"
  }
}
